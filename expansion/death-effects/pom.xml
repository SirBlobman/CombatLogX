<project>
    <modelVersion>4.0.0</modelVersion>
    <packaging>jar</packaging>

    <parent>
        <groupId>com.github.sirblobman.combatlogx.expansion</groupId>
        <artifactId>expansion-parent</artifactId>
        <version>11.0.0.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>death-effects</artifactId>
    <name>Expansion: Death Effects</name>
    <description>An expansion for CombatLogX that creates different effects when players die.</description>

    <properties>
        <spigot.version>1.13.2-R0.1-SNAPSHOT</spigot.version>
    </properties>

    <build>
        <finalName>Death Effects</finalName>
    </build>
</project>
