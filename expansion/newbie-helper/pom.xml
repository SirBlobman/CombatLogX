<project>
    <modelVersion>4.0.0</modelVersion>
    <packaging>jar</packaging>

    <parent>
        <groupId>com.github.sirblobman.combatlogx.expansion</groupId>
        <artifactId>expansion-parent</artifactId>
        <version>11.0.0.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>newbie-helper</artifactId>
    <name>Expansion: Newbie Helper</name>
    <description>An expansion for CombatLogX that prevents new players from being attacked.</description>

    <properties>
        <maven.deploy.skip>false</maven.deploy.skip>
    </properties>

    <build>
        <finalName>Newbie Helper</finalName>
    </build>
</project>
