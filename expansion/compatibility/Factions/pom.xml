<project>
    <modelVersion>4.0.0</modelVersion>
    <packaging>jar</packaging>

    <parent>
        <groupId>com.github.sirblobman.combatlogx.expansion.compatibility</groupId>
        <artifactId>compatibility-parent</artifactId>
        <version>11.0.0.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>Factions</artifactId>
    <name>Compatibility Expansion: MassiveCore Factions</name>
    <description>An expansion for CombatLogX that detects non-pvp areas from most plugins for Factions.</description>

    <build>
        <finalName>Factions Compatibility</finalName>
    </build>
</project>
