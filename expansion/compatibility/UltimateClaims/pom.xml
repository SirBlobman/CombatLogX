<project>
    <modelVersion>4.0.0</modelVersion>
    <packaging>jar</packaging>

    <parent>
        <groupId>com.github.sirblobman.combatlogx.expansion.compatibility</groupId>
        <artifactId>compatibility-parent</artifactId>
        <version>11.0.0.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>UltimateClaims</artifactId>
    <name>Compatibility Expansion: UltimateClaims</name>
    <description>An expansion for CombatLogX that detects non-pvp areas from the UltimateClaims plugin.</description>

    <repositories>
        <!-- SirBlobman Public -->
        <repository>
            <id>sirblobman-public</id>
            <url>https://nexus.sirblobman.xyz/repository/public/</url>
        </repository>
    </repositories>

    <dependencies>
        <!-- UltimateClaims -->
        <dependency>
            <groupId>com.songoda</groupId>
            <artifactId>UltimateClaims</artifactId>
            <version>1.7.0</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>UltimateClaims Compatibility</finalName>
    </build>
</project>
