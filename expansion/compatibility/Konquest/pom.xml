<project>
    <modelVersion>4.0.0</modelVersion>
    <packaging>jar</packaging>

    <parent>
        <groupId>com.github.sirblobman.combatlogx.expansion.compatibility</groupId>
        <artifactId>compatibility-parent</artifactId>
        <version>11.0.0.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>Konquest</artifactId>
    <name>Compatibility Expansion: Konquest</name>
    <description>An expansion for CombatLogX that detects non-pvp areas from the Konquest plugin.</description>

    <repositories>
        <!-- SirBlobman Private Repository -->
        <repository>
            <id>sirblobman-private</id>
            <url>https://nexus.sirblobman.xyz/repository/private/</url>
        </repository>
    </repositories>

    <dependencies>
        <!-- Konquest -->
        <!-- Requires local installation or private repository access -->
        <dependency>
            <groupId>konquest</groupId>
            <artifactId>Konquest</artifactId>
            <version>0.9.0</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>Konquest Compatibility</finalName>
    </build>
</project>
