name: "${expansionName}"
prefix: "${expansionPrefix}"
description: "${expansionDescription}"

main: "combatlogx.expansion.compatibility.region.crash.claim.CrashClaimExpansion"
version: "17.1"
author: "SirBlobman"

plugin-depend:
  - "CrashClaim"
