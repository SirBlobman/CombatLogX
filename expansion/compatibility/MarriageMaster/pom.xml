<project>
    <modelVersion>4.0.0</modelVersion>
    <packaging>jar</packaging>

    <parent>
        <groupId>com.github.sirblobman.combatlogx.expansion.compatibility</groupId>
        <artifactId>compatibility-parent</artifactId>
        <version>11.0.0.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>MarriageMaster</artifactId>
    <name>Compatibility Expansion: MarriageMaster</name>
    <description>An expansion for CombatLogX that adds compatibility for features in MarriageMaster.</description>

    <repositories>
        <!-- MarriageMaster Repository -->
        <repository>
            <id>pcgf-repo</id>
            <url>https://repo.pcgamingfreaks.at/repository/maven-everything/</url>
        </repository>
    </repositories>

    <dependencies>
        <!-- Marriage Master API -->
        <dependency>
            <groupId>at.pcgamingfreaks</groupId>
            <artifactId>MarriageMaster-API</artifactId>
            <version>2.5.3</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>MarriageMaster Compatibility</finalName>
    </build>
</project>
