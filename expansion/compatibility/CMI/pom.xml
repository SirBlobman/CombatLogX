<project>
    <modelVersion>4.0.0</modelVersion>
    <packaging>jar</packaging>

    <parent>
        <groupId>com.github.sirblobman.combatlogx.expansion.compatibility</groupId>
        <artifactId>compatibility-parent</artifactId>
        <version>11.0.0.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>CMI</artifactId>
    <name>Compatibility Expansion: CMI</name>
    <description>An expansion for CombatLogX that adds compatibility for features in CMI.</description>

    <repositories>
        <!-- SirBlobman Public Repository -->
        <repository>
            <id>sirblobman-public</id>
            <url>https://nexus.sirblobman.xyz/repository/public/</url>
        </repository>
    </repositories>

    <dependencies>
        <!-- CMI -->
        <dependency>
            <groupId>com.Zrips</groupId>
            <artifactId>CMI-API</artifactId>
            <version>8.7.8.2</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>CMI Compatibility</finalName>
    </build>
</project>
